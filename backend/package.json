{
  "name": "backend",
  "version": "1.0.0",
  "description": "Smart Campus Project Evaluation and Ranking System - Backend",
  "main": "app.js",
  "scripts": {
    "start": "nodemon app.js",
    "seed": "node -e \"require('mongoose').connect(require('./config').MONGODB_URI).then(()=>require('./seed')()).then(()=>process.exit(0))\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^9.9.1",
    "multer": "^2.2.0",
    "nodemon": "^3.1.14"
  }
}
